#Manage login console or gui check  /etc/issue;/etc/motd;/etc/issue.net (L102)

#/etc/motd is print after the login:ideal for communicate some information  and 
/etc/issue.net is printing before the login:ideal for warinings (specially for ssh protocol)

#about the sshd config :Remove the hash mark (#) from the line. If the word none is listed, instead of the issue 
.net file, change it to /etc/issue.net => "#banner none"  to  "banner /etc/issue.net"

+-----------------------------------------------------------+
+                       SEE ALSO                            +
+-----------------------------------------------------------+
L(106) => ubuntu case